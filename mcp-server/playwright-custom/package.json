{
  "name": "playwright-custom-mcp",
  "version": "1.0.0",
  "description": "Custom Playwright MCP server with test execution and DOM fetching",
  "type": "module",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "verify": "node -e \"import('@modelcontextprotocol/sdk/server/mcp.js').then(() => console.log('SDK OK')).catch(e => { console.error('SDK import failed:', e.message); process.exit(1); })\""
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.18.1",
    "playwright": "^1.40.0"
  }
}
